import React from 'react';

export const metadata = {
    title: "Sign up | Car Doctor Pro",
    description: "Car Repairing Workshop",
};

const signup = () => {
    return (
        <div>
            Registration Page
        </div>
    );
};

export default signup;